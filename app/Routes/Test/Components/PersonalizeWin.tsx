'use client'

//Bibliotecas externas
import { motion } from "framer-motion"

const PersonalizeOption: React.FC = () => {
    return (
        <div>
            OPÇÃO DE PERSONALIZAR SELECIONADA!
        </div>
    )}

export default PersonalizeOption